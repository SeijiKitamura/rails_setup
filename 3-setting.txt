# 以下のファイルをRailsアプリ直下にコピー

Gemfile

# Railsアプリのディレクトに移動して以下のコマンドを実行
bundle install

# 以下のファイルをRailsアプリのapp/assets/stylesheetsにコピー
custom.css.scss

# 以下のファイルをRailsアプリのapp/assets/javascriptsにコピー
application.js

# 以下のファイルをRailsアプリのapp/views/layoutsにコピー
application.html.slim

# 以下のファイルをRailsアプリのconfig/にコピー
puma.rb

# Railsアプリに以下のディレクトリを作成
tmp/sockets
tmp/pids

# 以下は本番環境構築

# /etc/nginx/conf.dに以下のファイルをコピー(root権限で)
test_app.conf

# 以下のファイルの内容をcronに登録(一般ユーザーで)
crontab.txt

# Railsアプリのディレクトリで以下のコマンドを実行
rails assets:precompile

# 再起動もしくは以下のコマンドでRails起動
cd /home/kennpin1/test_app &&
pumactl stop               &&
rails assets:precompile    && 
SECRET_KEY_BASE=`rake secret` RAILS_ENV=production bundle exec puma -C config/puma.rb -d
